version: 2
project_name: lalafo-pp-cli
changelog:
  disable: true
builds:
  - id: lalafo-pp-cli
    main: ./cmd/lalafo-pp-cli
    binary: lalafo-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X lalafo-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: lalafo-pp-mcp
    main: ./cmd/lalafo-pp-mcp
    binary: lalafo-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: lalafo-pp-cli
    repository:
      owner: 
      name: homebrew-tap
    homepage: "https://github.com//lalafo-pp-cli"
    description: "Undocumented REST API for lalafo.kg — Kyrgyz classifieds marketplace."
    install: |
      bin.install "lalafo-pp-cli"
      bin.install "lalafo-pp-mcp"
